---
- name: Ensure apt cache is up to date
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install common packages
  apt:
    name:
      - curl
      - git
      - python3
      - python3-venv
      - python3-pip
      - ca-certificates
    state: present
